version: '3.8'

services:
  biotool_nodeapp:
    build: .
    ports:
      - "5000:5000"
    env_file:
      - .env
    volumes:
      - /root/bacteriaAB/FastA:/app/FastA
    restart: unless-stopped
