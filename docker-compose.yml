version: '3.9'

services:
  frontend:
    build: ./
    container_name: react_frontend
    ports:
      - "3000:3000"
    environment:
      - REACT_APP_SERVER_URL=http://103.159.50.207:8080
    stdin_open: true
    tty: true
